import React from "react";

import { PropTypes, AppBar, Toolbar, Typography } from "material-ui";

export default props => (
  <AppBar position="static" color="default">
    <Toolbar>
      <Typography variant="title" color="#00000">
        212Data To-Do list e
      </Typography>
    </Toolbar>
  </AppBar>
);
